# 企业级供应商准入治理

供应商准入联合检查制裁名单、税务主体、收款账户、质量资质、数据保护、财务风险和业务连续性。关键供应商必须具备有效质量资质与连续性方案。

`POST /api/enterprise/scm/supplier-qualification` 返回 `APPROVE / CONDITIONAL / REJECT` 决策。生产环境应连接供应商主数据、第三方尽调、证书到期提醒、审批流和变更审计。
